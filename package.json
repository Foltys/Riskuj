{
  "name": "quiz-game",
  "version": "1.0.0",
  "description": "Multiplayer quiz game",
  "scripts": {
    "dev": "concurrently \"npm run --workspace server dev\" \"npm run --workspace client start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "tsx": "^4.20.3"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
